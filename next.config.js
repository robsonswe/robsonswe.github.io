/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['ross1996.github.io', 'localhost', 'picsum.photos'],
  },
}

module.exports = nextConfig
